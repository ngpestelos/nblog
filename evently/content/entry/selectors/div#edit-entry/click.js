function() {
  /*
  var id = $("#header h2").attr("data-id");
  var app = $$(this).app;
  var widget = $(this);
  app.db.openDoc(id, {
    success: function(doc) {
      widget.trigger("editor", [doc]);
    }
  });
  */
}